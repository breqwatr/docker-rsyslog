$ModLoad imudp 
$UDPServerRun 514 
$ModLoad imtcp 
$InputTCPServerRun 514 
$AllowedSender TCP, {{ senders }}
$AllowedSender UDP, {{ senders }}
$template remote-incoming-logs,"/var/log/networks/%HOSTNAME%.log" 
*.* ?remote-incoming-logs
$RepeatedMsgReduction on
