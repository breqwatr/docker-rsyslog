# breqwatr/docker-rsyslog

Containerized rsyslog server


## Environment variables

```

ALLOWEDSERVERS   (optional)    Defines comma-separated ip addresses of syslog clients.
                               Allows all IPs if this env variable isn't defined. 

```
